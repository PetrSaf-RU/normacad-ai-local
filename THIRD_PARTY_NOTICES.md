# Third-party notices

NormaCAD AI uses third-party components that retain their own licenses:

- Qwen2.5-Coder and Qwen2.5-VL model artifacts distributed through the
  `models-v1` GitHub Release. Their Ollama manifests include the upstream
  license layer.
- Ollama, installed separately by the setup script.
- Tesseract OCR and the `rus`, `eng`, and `osd` trained-data files.
- PdfPig and Microsoft.Data.Sqlite NuGet packages.

Review the upstream licenses before redistribution or commercial deployment.
